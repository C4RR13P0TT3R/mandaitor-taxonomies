{
  "name": "mandaitor-taxonomies",
  "version": "1.0.0",
  "private": true,
  "description": "Community-contributed industry taxonomies for the Mandaitor delegation registry",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "validate": "node scripts/validate-all.mjs",
    "clean": "pnpm -r clean",
    "generate:manifest": "node scripts/generate-manifest.mjs",
    "new-taxonomy": "node scripts/scaffold-taxonomy.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^2.0.0",
    "prettier": "^3.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "license": "Apache-2.0"
}
